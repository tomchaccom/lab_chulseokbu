name: frontend
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.8.1 # (Flutter SDK 3.8.1과 호환되는 Dart 3.x 환경)

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2
  cupertino_icons: ^1.0.8
  dio: ^5.9.0
  # ⭐️ 업데이트 필요: Riverpod 최신 안정 버전으로 변경
  flutter_riverpod: ^2.5.1 # 현재 3.x 버전은 없습니다. 2.5.x가 최신 안정 버전입니다.
  freezed_annotation: ^3.0.0 # freezed 2.5.x와 호환되는 최신 버전
  json_annotation: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # 👇👇👇 코드 생성 패키지 최신 안정 버전으로 통일 👇👇👇
  build_runner: ^2.4.9
  freezed: ^3.2.3
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true